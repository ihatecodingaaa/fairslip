# Defect classes

One line per recurring defect class. Not a bug list - a list of *kinds* of bug
that recur, so they can be prevented structurally rather than fixed repeatedly.

Format: `YYYY-MM-DD | class | where it appeared | structural prevention`

## Open

2026-09-06 | asserted-confidence | any LLM "confidence: 0.7" on an extracted field | status is derived from two-reader agreement or human confirmation; no numeric confidence exists in the schema
2026-09-06 | sanitiser-opens-bypass | block-destructive.ps1 stripped all single-quoted text, so sh -c 'rm -rf x' passed | any input-stripping step in a guard needs an evasion test alongside its false-positive test
2026-09-06 | fix-verified-by-inspection-only | the strip was reviewed as correct and shipped without an evasion probe | guard changes require an adversarial test case, not just the motivating case
2026-09-06 | check-disabled-by-absent-dependency | hooks silently no-op'd when jq was missing | a disabled check must announce itself (systemMessage) or fail closed; never pass quietly
2026-09-06 | inconsistent-worked-example | advisor's $7.87/h alongside $1,200 basic | every number on a slide must be produced by the engine; test_a_7_87_hourly_rate_implies_about_1500_basic guards the specific case

2026-09-06 | contested-secondary-source | PR Year-2 employer CPF rate: 9% in two sources, 8% in a third | a rate enters an engine only from the primary table; contested rates are OutOfScopeError until verified

2026-09-06 | check-red-for-the-wrong-reason | gate.ps1 preferred pip's pytest.exe shim, which Windows Smart App Control blocks; the gate reported pytest FAILED while all tests passed | resolve the project's own venv interpreter first and report which interpreter ran, so a wrong one is visible
2026-09-06 | overlapping-figures-summed | gross shortfall $62.24 and CPF shortfall $23 share the $12 employee CPF on the missing wage; adding them double-counts | the engine returns a non-overlapping split with two independent identities tested; the UI may only display its fields
2026-09-06 | duplicate-config-scope | 3 instances: (1) project hooks and permissions copied into ~/.claude/settings.json, firing twice here and erroring in every other project; (2) ~/.claude/rules/fairslip-domain.md kept a pre-reader-eligibility copy of the project rule, so the global scope taught the superseded field table; (3) cpf-rules.md and mom-pay-rules.md duplicated the same way (cpf-rules.md missing the shortfall-split section) | project-scoped config and rules live only in the repo; ~/.claude/ holds nothing named after a project. After any session that edits settings or rules, diff ~/.claude/rules against .claude/rules and check /hooks source labels

2026-09-07 | liveness-read-as-correctness | /health returns 200 with the engine broken; used as evidence production worked | a health check establishes only that the process answered; correctness needs an endpoint that exercises the engine
2026-09-07 | fix-applied-to-one-of-two-twins | gate.ps1 was fixed for the split layout and the pytest shim; gate.sh was not, so at the repo root it exits 0 having checked nothing | a guard with per-platform twins needs the same probe run against both, or one implementation
2026-09-07 | half-input-silently-dropped | rules.py:212-217 discards both rest-day facts when only one is supplied, with no flag; expected_gross then renders as if no rest day was worked | a partially supplied pair raises or flags; an input the engine cannot use must be expressed in the output
2026-09-07 | same-name-different-fact | payslip "Net pay" vs the amount received are distinct facts with one name; reader agreement on the printed figure would endorse the discrepancy the product exists to find | a field is reader-eligible only if what the document shows IS the fact needed
2026-09-07 | manifest-not-read-by-builder | backend/requirements.txt pins anthropic==1.4.0, but the Vercel build log says "Installing required dependencies from pyproject.toml"; a dep added only to requirements.txt imports fine locally and ImportErrors in production | runtime deps live in pyproject.toml [project.dependencies]; confirm the manifest from the build log, not from which file exists
2026-09-07 | established-status-mistaken-for-established-value | HUMAN_CONFIRMED says a person answered; it does not say the answer is a number the engine can compute with. Only the status was checked, so payInputsFrom minted HUMAN_CONFIRMED for any non-empty string: "NaN" and "Infinity" gave HTTP 500, net_paid="NaN" gave HTTP 200 and a screen reading "$NaN.undefined", ot_hours="-5" produced overtime of -$47.20, and days_per_week=7 computed a gross because the only guard lived inside daily_rate(), which a month with no rest day never calls | a status gate and a value gate are two checks. compute_expected() now runs both: _require_decimal (finite, expressible in cents, non-negative), _require_days_per_week (at the boundary, not inside the formula), _require_bool (bool("false") is True), _require_choice. 80 tests in backend/tests/test_rules_input_guards.py, derived over every amount field x every bad spelling. Put the guard where the suite is: the frontend has no test runner, so a frontend-only guard is held by review
2026-09-07 | guard-that-only-fires-on-some-paths | daily_rate() validated days_per_week correctly, but it is only called when a rest day was worked, so every month without one accepted any integer | validate an input where it ENTERS the engine, not where it is first used. A check reachable only down one branch is not a check on the input
2026-09-07 | missing-narrated-as-settled | /check said "Not asked: the documents show no hours worked on a rest day" whenever rest_day_hours was not established. On the real artwork reader A read 8 off a roster that says "Sun 14: full day (rest day)" and reader B read nothing, so the fact was MISSING - and the screen stated the opposite of what the document shows, on the part of the month that is most of the discrepancy. The cause was a two-state helper (restDayWorked: boolean) for a three-state world; the same collapse then nulled BOTH rest-day facts before /compute, so the engine was told "no rest day" rather than being allowed to refuse | absent, zero and unknown are three states and a boolean cannot hold them. Where a fact gates copy or an engine input, model it as an explicit union (worked | not_worked | unknown) so "unknown" has to be handled by name. Copy may describe what a reader said or what was established, never what "the documents show", unless the documents were established to show it. Sibling of ui-invents-a-cause: that one invented a cause for a refusal, this one invented a fact about a document
2026-09-07 | cached-path-wearing-a-live-timing | the reader chip printed the entry's STORED generation latency next to "from cache", so production read "from cache 3688 ms" for a replay that took about 1 ms. The inverse of fast-is-not-cached: there a live path wore a cached path's speed, here a cached path wore a live path's timing | a measurement rendered beside a status is read as describing the request in front of the viewer. Show a number only on the path that produced it - a stored figure needs its own label and date, or it does not go on screen
2026-09-07 | write-path-contradicts-its-own-contract | the cache was documented everywhere as "generated offline, committed to the repo, read-only in production" and implemented as write-at-runtime. On Vercel the bundle is read-only, so every write raised OSError into a bare `except OSError: pass` whose comment said a read-only filesystem must not fail a good reading - true, and it meant the cache was permanently empty and permanently silent about it. Locally the first call populated the cache for the second, so every test passed. No test compared the documented contract against the implemented one | when a doc states a lifecycle (who writes, when, where), a test must assert it - here: the request path writes nothing (tests/test_extract.py, test_extract_api.py), one deliberate script is the only writer, and the artefact actually EXISTS (tests/test_cache_is_populated.py). A swallowed exception on a path the contract says is never taken is a contradiction, not tolerance
2026-09-07 | precondition-untested-mechanism-tested | every cache test asked "would a hit work if an entry existed?" and none asked "does an entry exist?", so a cache with zero committed entries passed a suite full of green cache tests | for anything with a required artefact, guard the artefact's existence separately from the code that consumes it, and derive the required set from the inputs on disk so a new one cannot be forgotten. Skip loudly while the input is absent; fail the moment it appears
2026-09-07 | fast-is-not-cached | the second production call to /extract was 3.76s against 9.06s and looked cached; it was two live calls on warm connections. from_cache=false was the only thing telling the truth and nothing aggregated it | a response that came down a different path must SAY which path, at the top level, in words a screen shows. Never let a timing stand in for provenance
2026-09-07 | stale-doc-taken-as-ground-truth | CLAUDE.md and .claude/rules/fairslip-domain.md both said reader B goes "via OpenRouter", so extract.py was built against openrouter.ai with OPENROUTER_API_KEY. The actual model verification the day before called OpenAI directly with OPENAI_API_KEY, and there is no OpenRouter account - the reader could never have worked, and would have presented as a reader outage on every run | a doc describing an integration is a claim about the world, not evidence of it; before building against it, check the thing it describes (which key exists, what the last verified run actually called). Docs were corrected at the same time as the code so the next session does not repeat it
2026-09-07 | shared-dependency-defeats-the-redundancy | the same OpenRouter mistake had a second edge: routing BOTH readers through one aggregator gives them one account, one balance and one outage, which is exactly the shared failure the second reader exists to rule out | when a component exists to provide independence, check the independence holds all the way down - two models behind one vendor, one gateway or one key are one reader wearing two labels
2026-09-07 | OPEN DESIGN QUESTION, not yet a defect | /check runs the Employment Act pack but not the CPF pack, because /cpf needs declared_ow - the wage the employer actually computed CPF on - and no uploaded document states it. It could be inferred from the payslip's employee-CPF line (280 / 20% = 1400), but CPF drops cents on the employee share, so the honest answer is a RANGE ($1,400.00-$1,404.99), not a value. Shipping a point estimate would be the exact failure this project exists to prevent | Stage 3: either an engine function returning the range with both bounds, or the CPF pack stays on fixture data. Until then /check says on screen that declared_ow is not stated by any document. Do not let a frontend compute it
2026-09-07 | published-choice-rejected-by-its-own-server | /extract publishes is_workman choices with the values "true"/"false", and app/main.py's _as_bool accepted only a JSON bool - so a worker picking the option the server had just offered was refused with "is_workman: 'true' is not true or false". The schema tests checked the choice values against the engines' signatures but never sent one back through the API | when a server publishes a set of accepted values, a test must feed every published value back through the endpoint that consumes it; checking the list against a function signature is not the same as checking the round trip
2026-09-07 | ui-verification-blocked-by-tooling | the Claude browser extension was not connected, so the screenshot rule could not be met the usual way; the Next.js dev server's HMR socket also failed under headless Chrome, leaving the page rendered but not hydrated - which looked like a UI bug and was not | verify UI against a production build (next build && next start), and drive it over CDP with Node's built-in WebSocket when the extension is unavailable; a page that renders but does not respond is a hydration question first

2026-09-07 | OPEN, NOT ON THE DEMO PATH (deliberate, 7 Sept) | five known defects left unfixed to keep one focused pass: (1) gate.sh checks nothing at the repo root - the split-layout fix went into gate.ps1 only, so on a POSIX box the gate is green having run nothing; (2) rules.py drops BOTH rest-day facts when one is None, with no flag (half-input-silently-dropped, still open in the engine - the frontend now routes around it); (3) an unreachable `typeof raw === "object"` guard in check/page.tsx whose comment credits it with handling DISAGREED, which isEstablished already excluded; (4) "the documents do not contain them" over-claims for days_per_week when a KET was uploaded, and "no document you uploaded states that figure" over-claims for declared_ow - both are claims about documents FairSlip never read for those fields; (5) INVALID_INPUT vs UNESTABLISHED_INPUT is inconsistent - "abc" is caught by the wire converter and reported as INVALID_INPUT while "NaN" reaches the engine and is reported as UNESTABLISHED_INPUT, though a worker did the same thing in both cases | revisit after Stage 3. None is on the demo path; all are real

2026-09-07 | DECISION, not a defect: accusatory-default-on-unestablished-input | verify() compares month 2 against month 1. The domain contract names three verdicts - CORRECTED / PARTIALLY_CORRECTED / NOT_CORRECTED - and all three are assertions about the EMPLOYER'S CONDUCT in month 2. When month 2 cannot be established at all (a reader disagreement, a MISSING field, a worker typing something that is not a number), none of the three is available. The tempting fallback is NOT_CORRECTED, and it is the worst of the three: it says "they did not fix it" on input the system never established, which is the governing rule inverted, and it fails in the ACCUSATORY direction. On a product for vulnerable workers that is the worst available failure - it would put a worker into a confrontation with their employer on the strength of a bad photograph | a fourth outcome, UNVERIFIABLE, RETURNED (not raised) as a distinct type carrying blocked_by: the name and Status of every field that blocked it, so the screen says WHICH fact is missing rather than that something is (sibling of missing-narrated-as-settled). It deliberately carries NO month-2 arithmetic - it computed none, and a zero would read as "nothing outstanding". UNVERIFIABLE is also NOT TERMINAL in the state machine: a worker whose month-2 payslip photographed badly has not reached the end of anything, so it routes back to AWAITING_NEXT_PAYSLIP and VERIFYING. Enforced by tests/test_agent_verify.py (Unverifiable is a separate dataclass with no remaining_gap attribute, derived over every required PayInputs field) and tests/test_agent_mandate.py::test_unverifiable_is_not_terminal_and_can_be_re_attempted

2026-09-07 | LIMITATION, stated not hidden: mandate-is-a-runtime-chokepoint-not-a-type-guarantee | it is tempting to describe the mandate as "enforced by the type system" because allowed_actions(level) is a pure function and Mandate.act() is the single dispatch point. It is not. The level arrives as data - a JSON field from a browser - so Python cannot make an ungranted action UNREPRESENTABLE at type-check time; what exists is one runtime guard on one path. Worse, and separate: THIS BUILD HAS NO AUTHENTICATION, so nothing establishes that the worker granted the level the caller claims. A guard against the software exceeding a level is not a guard against a person claiming one. Overstating either would be the asserted-confidence class applied to an architecture claim rather than to a field | say it in three places rather than one: fairslip/agent.py's module docstring states it literally ("It does not give you a proof. Do not describe it as one."); NO_AUTHENTICATION_NOTICE is a module constant; and GET /agent/mandate returns that notice in its body so a SCREEN can show it, because a judge asking "what stops someone else setting level 4" deserves the answer on screen and not in a docstring. tests/test_agent_never_files.py::test_the_module_declares_no_authentication_and_says_so and tests/test_agent_api.py::test_mandate_endpoint_states_that_this_build_has_no_authentication hold it. The structural half that IS real is narrower and is claimed as such: Sent.tap and TapEvent.at have no defaults, so a SENT record without a tap is unconstructible

2026-09-07 | contested-secondary-source (second instance) | the project brief and .claude/rules/fairslip-domain.md both stated "CPF's 1-year refund/adjustment limit" and the agent was to surface it as a DEADLINE beside TADM's real filing limits. CPF Board's own page says something different in kind: "Please note the likelihood of recovery for any non/underpayment of CPF contributions beyond one year is low." That is a statement about ODDS, not a limitation period, and CPF's enforcement page (last updated 05 Aug 2026) states no limitation period at all. Rendering it as a deadline would have told a worker their claim expires when the published source does not say so - and would have done it in the direction that discourages a valid claim. The source here was MY OWN BRIEF, which is the point: a contested secondary source is not always a third-party website, it can be the instruction you were handed | verified against the primary page 7 Sept 2026 (cpf.gov.sg/service/article/how-can-i-lodge-a-report..., last updated 12 Mar 2026) before any of it reached a screen. Both .claude/rules/cpf-rules.md and fairslip-domain.md now carry the sentence verbatim with its URL and page date, and both say explicitly that it is likelihood and not entitlement. TADM's limits, which ARE real, were re-verified the same day on MOM's managing-employment-disputes page (last updated 26 March 2026): 1 year while employed, 6 months after leaving

2026-09-07 | UNESTABLISHED, deliberately absent from every screen: tadm-claim-value-caps | MOM's page says "Up to $20,000" for a standard claim and "$30,000 if you go through Tripartite Mediation Framework or mediation assisted by unions recognised by the Industrial Relations Act". TADM's own filing page describes $20,000 per individual claim with $40,000 combined across claim types for non-union members. Those are different framings of what composes with what, and I could not establish from either page how a single worker's cap is actually determined. A cap shown wrongly either understates what a worker may claim or overstates it - both are harmful and neither is recoverable at a mediation counter | no claim cap appears anywhere in the product. Not in REFERENCE_LINKS copy, not in the escalation pack, not on the panel. When escalation content is built, the cap is either verified against a single authoritative statement or the screen links the worker to TADM and says nothing about amounts. Recorded here so the next session does not "helpfully" fill it in from memory

2026-09-07 | generator-reports-failure-for-work-that-succeeded | scripts/make_draft_entry.py wrote the Bengali draft entry to disk correctly and then died with UnicodeEncodeError printing it, because a Windows console defaults to cp1252. Exit code was non-zero and the traceback was the last thing on screen, so the run read as a failure while the artefact it produced was sitting on disk, valid and complete. The inverse of a silent failure: a loud failure for a silent success | a script whose OUTPUT is in a non-Latin script must set its stream encoding before it prints anything (sys.stdout.reconfigure(encoding="utf-8", errors="replace")). More generally: reporting is not free of failure modes, and a write that succeeded must not be reported by a print that cannot

2026-09-07 | fixture-persona-rendered-as-the-viewers-own | switching the agent panel to Mei Ling put a CPF shortfall card ($62.24 / $12 / $50.24 / $23 / $11 / $73.24) directly beneath the viewer's OWN reconciliation - and about forty lines above it the same page says "The CPF side of this month is not shown ... no document you uploaded states that figure ... so it shows none." A worker whose payslip showed a $200 difference would still have seen $62.24, presented as this month's CPF consequence. The screen answered the open declared_ow design question with a fixture, and contradicted itself doing it. The backend was already serving `persona: "Mei Ling"` and the frontend rendered it nowhere - the one field that would have disambiguated whose month it was got dropped on the floor | a card built from fixture data must SAY whose data it is, in its first line, before any figure. The panel now leads with "FICTIONAL WORKED EXAMPLE - NOT YOUR FIGURES", names the invented worker, sits inside a dashed border rather than the solid ones used for the viewer's own results, and refuses to render at all unless the server sent BOTH the pack and the sentence that says whose it is (two fields with independent defaults can drift). Sibling of missing-narrated-as-settled: that one invented a fact about a document, this one attached a real fact to the wrong person

2026-09-07 | fixture-depicts-a-fix-it-does-not-actually-model | mei_ling_month2_corrected pays the $62.24 as cash and leaves deductions_total at $280 - which is 20% of $1,400 with cents dropped, i.e. the employer is STILL contributing CPF on the wrong wage in month 2. The arrears is itself CPF-liable Ordinary Wage, so a genuinely corrected month carries a larger employee CPF line and a different net. The fixture was docstringed as "after the employer responded", and the screen returns CORRECTED for it while the panel above says $23 is missing from her CPF account | the fuller correction is deliberately not modelled: arrears paid in a later month raise the Additional Wage question, and v1 is Ordinary Wage only - the CPF engine refuses AW rather than approximating it. So the fixture is honest about one pack and silent about the other, and its docstring now says exactly that, names the $280 line, and points at the AW limitation. A demo fixture is a claim about what a document looks like; when it is only partially faithful, the docstring is where that is recorded, not left for a reviewer to derive

2026-09-07 | drafted-message-claims-what-a-document-shows | the committed draft for Rahim opened "My payslip shows: - Basic: $1,200.00 - Overtime: $169.93 - Rest day pay: $92.31. This comes to $1,462.24 after deductions." The overtime and rest-day figures were RECONSTRUCTED by compute_expected from MOM's rules, and the prop payslip prints neither the rest-day pay nor the $1,462.24 - a payslip that showed them would not be in dispute. So the first sentence of a first-person message a worker is invited to SEND THEIR EMPLOYER misdescribed their own payslip. The draft guard could not see it: it checks forbidden vocabulary and unknown dollar amounts, and every figure here was real and every word permitted. Mei Ling's draft happened to say "I worked out" and was fine, so one of two committed artefacts was wrong and the suite was green | a claim about a document is a distinct failure from a forbidden word, so it gets its own check: FORBIDDEN_DOCUMENT_CLAIMS in fairslip/agent.py, applied in _build_draft (which runs on cache replay too, not only on generation), plus prompt rule 6 forbidding it, plus a test derived over the phrase list AND a test that reads the committed artefacts on disk. DRAFT_TEMPLATE_VERSION bumped so both entries regenerated under the new rule

2026-09-07 | committed-artefact-not-actually-committed | the draft cache entry for the DEFAULT persona sat untracked in the working tree. Every cache test was green - the file existed on disk, so is_file() passed - and the failure message of the test guarding it reads "an entry that is not committed does not exist in production", which is exactly the thing it could not check. It survived because deploys were run from the working tree; a fresh clone or a git-based deploy would have served DRAFT_UNAVAILABLE on the 0:55 beat. A second, subtler half: bumping the template version and changing a language had left two ORPHANED entries that no spec claimed, still tracked and still shipping | test_every_committed_entry_is_actually_TRACKED_BY_GIT shells out to `git ls-files --error-unmatch` per declared spec (skipping loudly if git is absent, never passing quietly), and test_the_cache_directory_holds_no_entry_no_spec_claims asserts the directory contents equal the declared set exactly - so an orphan fails as loudly as a missing entry. Third instance of precondition-untested-mechanism-tested: guarding that an artefact EXISTS is not guarding that it SHIPS

2026-09-07 | script-as-untested-surface | docs/demo-script.md contradicted the screen in twelve places at once: a reader disagreement the committed cache does not produce, a language the panel does not serve, a printed net pay the fixture does not use, a tap on a component tree that is already open, two beats that need a mandate level the script never raises, a roster upload the script never mentions, "MISSING" for a chip that says "not established", a control called "help me raise this" that does not exist, one narrated answer where the gate demands six, and a figure (1.35 days of basic pay) that no endpoint returns. Every one is a sentence a presenter says out loud, in a pitch whose thesis is that the system does not assert what it has not established | the script is a BUILD ARTIFACT and is now labelled as one in its own first paragraph, with each corrected beat carrying a short note saying what the screen actually does and why the old line was wrong - so the next person cannot restore it without reading the reason. Verify it against the running surface whenever a surface changes; the beats that state a number are the ones to check first

2026-09-08 | gloss-inherits-the-authoritys-attribution | the escalation screen renders a list under the heading "What CPF Board does say:". Each item was ONE STRING containing a verbatim quote and FairSlip's commentary on it, in the same font, size and colour, separated only by a pair of quotation marks. So FairSlip's inference was attributed to CPF Board by the heading above it. What it inferred made it worse: from "The Board will compute the CPF contributions ... once TADM has concluded your claims" - a sentence about WHEN an amount is computed - it concluded that "the TADM half above is the half to take first" and that a pre-filled CPF report "would not be the next step". That is advice, on a page whose entire purpose is telling members how to lodge that report, and it is WRONG for the worker who needs it most: the deadlines quoted four lines above give 6 months after leaving to file at TADM, so a worker past that window cannot use TADM at all and the CPF report is the route that remains. Discouraging a valid claim is the direction docs/debt.md already names as the worst one | the codebase already had the right shape and did not use it: EvidenceItem keeps `quoted` and `note` apart precisely so "a reader can always see which words are the authority's". what_is_known is now tuple[QuotedSource, ...] with the same split, the panel renders the note on its own line prefixed "FairSlip's reading, not theirs:", and the advice is deleted rather than reworded. A test asserts no note or quote contains "half to take first", "next step", "you should" or "do not lodge". NotBuilt's source_url/source_label lost their defaults and __post_init__ raises when a block quotes an authority without naming the page - both were optional while both siblings were required, so four unattributed quotations were constructible

2026-09-08 | quote-test-passes-by-construction | test_every_cpf_quote_on_the_escalation_screen_is_on_the_published_page checked, for each sentence in a hand-maintained tuple, that it appears in the API response. Both sides are code in the same repo, and a commit that adds a quotation to both at once passes by construction - which is exactly what the commit adding the sequence sentence did. The test could not catch a fabricated quote, which is the only thing it exists to catch | assert BOTH directions. The recorded sentences must reach the screen, and - the half that was missing - nothing rendered inside quotation marks may be absent from the recorded set. The recorded set is the thing a human re-verifies against the page before the pitch; cpf-rules.md now carries all four sentences with the URL, the page's last-updated date, and the date they were read in a browser

2026-09-08 | copy-contract-enforced-on-one-surface-only | FORBIDDEN_WORDS (owed, underpaid, breach, entitled, illegal, resolved, "must pay", "you must") was applied inside the draft guard and nowhere else. The escalation pack - the screen a worker reads when deciding what to do next, and the one carrying quotations from two governments - had no automated copy check at all. Its copy had been correct only because it was written carefully and reviewed twice | check_escalation_copy() runs over every FairSlip-AUTHORED string in the pack (26 of them: heading, disclaimer, every filing step, every note and source label, every not_built field including the notes on its quotes), derived from the pack's own shape so a field added later is covered or the loudness test fails. It runs inside prepare_escalation() on the way out, so a word added to a distant constant still cannot reach a screen. A positive control derived over FORBIDDEN_WORDS plants each word and asserts it is caught. `quoted` fields are deliberately EXCLUDED and the exclusion is the point: an authority may use a word FairSlip may not, and filtering a quotation would either censor a government or push someone to trim a quote until it passed - which is precisely how "beyond one year is low." shipped with a full stop

2026-09-08 | deadline-shown-without-its-look-back | the escalation screen showed TADM's two filing deadlines and not the third sentence on the same page: "Your claims cannot be earlier than 1 year from the date of filing." Nothing on screen was false. But a deadline answers WHEN YOU MAY FILE and the look-back answers HOW FAR BACK A FILED CLAIM MAY REACH, and a worker shown only the first, on the screen that tells them what to do next, has an incomplete picture of what they can recover | rendered as a third Deadline entry, verbatim, and SCOPED AS THE PAGE SCOPES IT: TADM prints the sentence inside the "If you have left employment" bullet and the still-in-employment bullet does not repeat it, so the label says "if you have left" and the source names TADM's page rather than MOM's, which does not carry the sentence at all. TADM's own worked example for the still-employed case implies a similar reach; an implication is not what the page states, and the pack does not extend it. Both pages' quotes now live verbatim in .claude/rules/mom-pay-rules.md with URLs and page dates - they had lived only in agent.py behind a code comment, so a RE-VERIFY BEFORE THE PITCH pass reached them by luck rather than by design

2026-09-08 | input-consumed-but-not-recorded | building the impact-radius view exposed a provenance gap in the engine. Both rest-day components consume `normal_daily_hours` - the rest-day table brackets on HALF of it, and rest_day_overtime computes `excess = rest_day_hours - normal_daily_hours` - and neither listed it in Component.inputs. One of them prints "{rdh}h of {ndh}h" in its own formula string while omitting ndh from its inputs tuple. Nothing had ever read `inputs` AS A GRAPH, so an under-recorded edge cost nothing and stayed invisible | the invariant "a MOVED line must carry the source of a fact that changed", asserted over every PayInputs field (tests/test_impact.py). It failed on normal_daily_hours the first time it ran. `inputs` is now a load-bearing record rather than display text, so anything the engine consumes must be listed - and the API reports `unexplained_moves` rather than smoothing over a disagreement between what it computed and what it recorded

2026-09-08 | detector-blinded-by-a-shared-key | `depends_on_changed` is derived by intersecting a component's recorded sources with the sources of facts that changed. Nothing required those strings to be DISTINCT, and Fact.source defaults to "". Give every fact one source - "payslip.jpg", or the empty default - and every line reports depends_on_changed=True, so `unexplained_moves` can never fire. The detector then reports green because it has been BLINDED, not because the graph is sound, and no test saw it because the fixture happens to carry nine unique strings | the run is REFUSED when a changed fact's provenance is shared with another fact, naming the field and the string. A view whose whole claim is "these lines did not move, and here is why" must not answer when it cannot tell which line is which. Sibling of check-disabled-by-absent-dependency: there the check was missing, here it was present and unable to see

2026-09-08 | reason-hardcoded-under-a-computed-claim | the impact view computed "N did not move" correctly and then printed, under EVERY held line, "did not list the fact you changed among its inputs" - a hardcoded sentence that never read `depends_on_changed`, the field computed for exactly that purpose. It is false whenever a line lists the changed fact and holds its value anyway: change normal_daily_hours 8 -> 9 and rest_day does precisely that. The screen stated, about a named line, something the same JSON response contradicted - reachable in one click on the shipped fixture, on the surface whose entire point is that its claims are computed. The suite was green because only the MOVED direction was asserted; the UNCHANGED direction, which is the one the copy asserts, had no test at all | the backend returns `held_but_dependent` and the screen branches on it, so the truer and stronger sentence appears: "listed the fact you changed among its inputs, and still came out the same". Tests assert the classification over every field in both directions. General rule: when a response carries a field computed to justify a claim, the screen must render THAT FIELD - a paraphrase beside a computed value is an assertion, and it will eventually disagree with it

## Carried from Keepsake / Orkestr

| equal-objects-different-canonical-forms | float vs Decimal, "18" vs "18.0", naive vs aware datetimes | compare Decimals after normalisation; never compare strings for numeric fields
| VIOLATED-without-establishment | a flag raised on a value that was never checked | flags are only raised inside compute_expected on established inputs
| quantified-test-name-single-instance | "test_all_fields_have_source" checking one field | derive cases from the dataclass fields or parametrize

## Resolved

ui-invents-a-cause | fixed 2026-09-07: page.tsx Refused and CpfPanel, and the new check page, all branch on refusal.error instead of asserting one cause. NOT enforced by a test - there is no frontend test runner in this project yet, so this one is held by review
route-table-drift | fixed 2026-09-07: /extract now has a handler, and the /demo(/.*)? over-capture was narrowed to /demo/fixtures. ENFORCED BY backend/tests/test_routes_match_vercel.py, which derives its cases from vercel.json's own rewrite sources and the registered FastAPI routes and fails on either an under-capture or an over-capture
appended-pattern-cancels-earlier-exception | fixed 2026-09-07: the duplicate .env* block was removed from the root .gitignore and the secrets block restated last, with the same exception added to frontend/.gitignore. VERIFIED with git check-ignore -q on backend/.env.example (not ignored) and backend/.env (ignored)
