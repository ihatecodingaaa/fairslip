# Defect classes

One line per recurring defect class. Not a bug list - a list of *kinds* of bug
that recur, so they can be prevented structurally rather than fixed repeatedly.

Format: `YYYY-MM-DD | class | where it appeared | structural prevention`

## Open

2026-09-06 | asserted-confidence | any LLM "confidence: 0.7" on an extracted field | status is derived from two-reader agreement or human confirmation; no numeric confidence exists in the schema
2026-09-06 | sanitiser-opens-bypass | block-destructive.ps1 stripped all single-quoted text, so sh -c 'rm -rf x' passed | any input-stripping step in a guard needs an evasion test alongside its false-positive test
2026-09-06 | fix-verified-by-inspection-only | the strip was reviewed as correct and shipped without an evasion probe | guard changes require an adversarial test case, not just the motivating case
2026-09-06 | check-disabled-by-absent-dependency | hooks silently no-op'd when jq was missing | a disabled check must announce itself (systemMessage) or fail closed; never pass quietly
2026-09-06 | inconsistent-worked-example | advisor's $7.87/h alongside $1,200 basic | every number on a slide must be produced by the engine; test_a_7_87_hourly_rate_implies_about_1500_basic guards the specific case

2026-09-06 | contested-secondary-source | PR Year-2 employer CPF rate: 9% in two sources, 8% in a third | a rate enters an engine only from the primary table; contested rates are OutOfScopeError until verified

2026-09-06 | check-red-for-the-wrong-reason | gate.ps1 preferred pip's pytest.exe shim, which Windows Smart App Control blocks; the gate reported pytest FAILED while all tests passed | resolve the project's own venv interpreter first and report which interpreter ran, so a wrong one is visible
2026-09-06 | overlapping-figures-summed | gross shortfall $62.24 and CPF shortfall $23 share the $12 employee CPF on the missing wage; adding them double-counts | the engine returns a non-overlapping split with two independent identities tested; the UI may only display its fields
2026-09-06 | duplicate-config-scope | 3 instances: (1) project hooks and permissions copied into ~/.claude/settings.json, firing twice here and erroring in every other project; (2) ~/.claude/rules/fairslip-domain.md kept a pre-reader-eligibility copy of the project rule, so the global scope taught the superseded field table; (3) cpf-rules.md and mom-pay-rules.md duplicated the same way (cpf-rules.md missing the shortfall-split section) | project-scoped config and rules live only in the repo; ~/.claude/ holds nothing named after a project. After any session that edits settings or rules, diff ~/.claude/rules against .claude/rules and check /hooks source labels

2026-09-07 | liveness-read-as-correctness | /health returns 200 with the engine broken; used as evidence production worked | a health check establishes only that the process answered; correctness needs an endpoint that exercises the engine
2026-09-07 | fix-applied-to-one-of-two-twins | gate.ps1 was fixed for the split layout and the pytest shim; gate.sh was not, so at the repo root it exits 0 having checked nothing | a guard with per-platform twins needs the same probe run against both, or one implementation
2026-09-07 | half-input-silently-dropped | rules.py:212-217 discards both rest-day facts when only one is supplied, with no flag; expected_gross then renders as if no rest day was worked | a partially supplied pair raises or flags; an input the engine cannot use must be expressed in the output
2026-09-07 | same-name-different-fact | payslip "Net pay" vs the amount received are distinct facts with one name; reader agreement on the printed figure would endorse the discrepancy the product exists to find | a field is reader-eligible only if what the document shows IS the fact needed
2026-09-07 | manifest-not-read-by-builder | backend/requirements.txt pins anthropic==1.4.0, but the Vercel build log says "Installing required dependencies from pyproject.toml"; a dep added only to requirements.txt imports fine locally and ImportErrors in production | runtime deps live in pyproject.toml [project.dependencies]; confirm the manifest from the build log, not from which file exists
2026-09-07 | stale-doc-taken-as-ground-truth | CLAUDE.md and .claude/rules/fairslip-domain.md both said reader B goes "via OpenRouter", so extract.py was built against openrouter.ai with OPENROUTER_API_KEY. The actual model verification the day before called OpenAI directly with OPENAI_API_KEY, and there is no OpenRouter account - the reader could never have worked, and would have presented as a reader outage on every run | a doc describing an integration is a claim about the world, not evidence of it; before building against it, check the thing it describes (which key exists, what the last verified run actually called). Docs were corrected at the same time as the code so the next session does not repeat it
2026-09-07 | shared-dependency-defeats-the-redundancy | the same OpenRouter mistake had a second edge: routing BOTH readers through one aggregator gives them one account, one balance and one outage, which is exactly the shared failure the second reader exists to rule out | when a component exists to provide independence, check the independence holds all the way down - two models behind one vendor, one gateway or one key are one reader wearing two labels
2026-09-07 | OPEN DESIGN QUESTION, not yet a defect | /check runs the Employment Act pack but not the CPF pack, because /cpf needs declared_ow - the wage the employer actually computed CPF on - and no uploaded document states it. It could be inferred from the payslip's employee-CPF line (280 / 20% = 1400), but CPF drops cents on the employee share, so the honest answer is a RANGE ($1,400.00-$1,404.99), not a value. Shipping a point estimate would be the exact failure this project exists to prevent | Stage 3: either an engine function returning the range with both bounds, or the CPF pack stays on fixture data. Until then /check says on screen that declared_ow is not stated by any document. Do not let a frontend compute it
2026-09-07 | published-choice-rejected-by-its-own-server | /extract publishes is_workman choices with the values "true"/"false", and app/main.py's _as_bool accepted only a JSON bool - so a worker picking the option the server had just offered was refused with "is_workman: 'true' is not true or false". The schema tests checked the choice values against the engines' signatures but never sent one back through the API | when a server publishes a set of accepted values, a test must feed every published value back through the endpoint that consumes it; checking the list against a function signature is not the same as checking the round trip
2026-09-07 | ui-verification-blocked-by-tooling | the Claude browser extension was not connected, so the screenshot rule could not be met the usual way; the Next.js dev server's HMR socket also failed under headless Chrome, leaving the page rendered but not hydrated - which looked like a UI bug and was not | verify UI against a production build (next build && next start), and drive it over CDP with Node's built-in WebSocket when the extension is unavailable; a page that renders but does not respond is a hydration question first

## Carried from Keepsake / Orkestr

| equal-objects-different-canonical-forms | float vs Decimal, "18" vs "18.0", naive vs aware datetimes | compare Decimals after normalisation; never compare strings for numeric fields
| VIOLATED-without-establishment | a flag raised on a value that was never checked | flags are only raised inside compute_expected on established inputs
| quantified-test-name-single-instance | "test_all_fields_have_source" checking one field | derive cases from the dataclass fields or parametrize

## Resolved

ui-invents-a-cause | fixed 2026-09-07: page.tsx Refused and CpfPanel, and the new check page, all branch on refusal.error instead of asserting one cause. NOT enforced by a test - there is no frontend test runner in this project yet, so this one is held by review
route-table-drift | fixed 2026-09-07: /extract now has a handler, and the /demo(/.*)? over-capture was narrowed to /demo/fixtures. ENFORCED BY backend/tests/test_routes_match_vercel.py, which derives its cases from vercel.json's own rewrite sources and the registered FastAPI routes and fails on either an under-capture or an over-capture
appended-pattern-cancels-earlier-exception | fixed 2026-09-07: the duplicate .env* block was removed from the root .gitignore and the secrets block restated last, with the same exception added to frontend/.gitignore. VERIFIED with git check-ignore -q on backend/.env.example (not ignored) and backend/.env (ignored)
